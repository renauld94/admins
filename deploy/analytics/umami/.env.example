# Copy to .env and set strong secrets
POSTGRES_PASSWORD=change-me-strong
APP_SECRET=use-a-64-byte-random-string
UMAMI_ADMIN_USER=admin@example.com
UMAMI_ADMIN_PASSWORD=ChangeMeSuperStrong

# Optional: public hostname for reverse proxy (nginx/traefik)
PUBLIC_HOST=analytics.simondatalab.de
