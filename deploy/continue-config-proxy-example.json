{
  "contextProviders": [
    {
      "name": "openwebui-staging-mcp",
      "type": "mcp",
      "params": {
        "serverUrl": "http://127.0.0.1:8001",
        "timeout": 30
      }
    }
  ],
  "authToken": "",
  "notes": "Set your real CF client id/secret as environment variables for the proxy. The proxy will add the headers to outbound requests."
}
