# Metrics Proxy configuration
PROMETHEUS_BASE_URL=http://10.0.0.150:9090
PORT=8088
ALLOWED_ORIGINS=https://www.simondatalab.de
# Comma-separated list of allowed PromQL queries (exact match)
ALLOWED_QUERIES=up,sum(rate(http_requests_total[5m]))
