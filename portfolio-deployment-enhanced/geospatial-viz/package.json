{
  "name": "geospatial-viz",
  "version": "1.0.0",
  "description": "",
  "main": "globe-threejs.js",
  "scripts": {
    "build": "esbuild globe-threejs.js --bundle --minify --sourcemap --outfile=dist/globe-bundle.js --format=esm",
    "start": "python3 -m http.server 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
