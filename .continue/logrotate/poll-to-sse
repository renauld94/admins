# Rotate poll-to-sse (gunicorn) logs kept in ~/.local/state or a configurable path.
# Install by copying this into /etc/logrotate.d/ or refer to these settings.

/home/simon/.local/state/poll-to-sse/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    create 0640 simon adm
}
